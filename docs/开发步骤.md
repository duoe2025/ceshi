# 牧童大卫 · 开发步骤进度看板（实时打勾）

> 这是「该开发什么 / 已完成什么 / 进度多少」的**实时进度清单**。规则：
> - 每完成一项就把 `[ ]` 改成 `[x]`，并**立刻 `git push`** 到 GitHub，避免丢失。
> - 设计/规格细节见 `docs/开发计划.md`（权威规格）；本文件只管**执行步骤与勾选状态**。
> - 状态图例：`[x]` 已完成 · `[~]` 进行中 · `[ ]` 待办。
> - 每个大阶段给出「验收标准」，全部勾选且验收通过才算该阶段完成。

**最后更新**：2026-05-30 · **当前阶段**：M3 多场景互通大世界骨架 ✅ 100% 完成 → 「完全迁移到 Phaser」三阶段(M1/M2/M3)全部收官；下一步 M4 HeroEngine 英雄卡 / M6 第二章「歌利亚」

---

## 总进度

| 里程碑 | 范围 | 进度 |
|---|---|---|
| M0 基础 Demo（第一章 + ARPG + 暗黑式数值/装备/特效 + 鼠标操作） | PR #1~#4 | ✅ 100% |
| **M1 完全 Phaser 原生化 · 阶段1（渲染/相机/多场景基座）** | PR #5 | ✅ 100% |
| **M2 完全 Phaser 原生化 · 阶段2（粒子 + 相机特效）** | ✅ 完成 | ✅ 100% |
| **M3 完全 Phaser 原生化 · 阶段3（多场景互通大世界骨架）** | PR #5 | ✅ 100% |
| M4 HeroEngine 英雄卡 + 带兵同伴 | 待办 | ⬜ 0% |
| M5 WorldEngine 互通世界 / 切图 / 解锁 | 待办 | ⬜ 0% |
| M6 QuestEngine 主线事件 + 第二章「歌利亚」 | 待办 | ⬜ 0% |
| M7 内容扩充（妻子/勇士相遇、先知、章节剧情至大卫年老） | 待办 | ⬜ 0% |

> 进度% = 该里程碑下已勾选子项 / 总子项。

---

## M0 · 基础 Demo（已完成）

- [x] 第一章「牧童大卫」：像素牧场地图 + 移动/碰撞 + NPC 对话（PR #1）
- [x] 三武器：弹弓（远程）/ 杖杆（近战）/ 弹琴（赞美驱邪灵）
- [x] 任务流程：找回 3 只羊 → 击退猛狮 → 夜战邪灵（弹琴门控）→ 结算
- [x] ARPG 即时战斗（取代回合制）：边跑边打、可逃跑、怪物 AI
- [x] 暗黑式数值系统：四主属 + 派生属性 + 命中/暴击/闪避/护甲减伤 + 经验升级
- [x] 暗黑式装备系统：稀有度 / 词缀 / 物品等级 / 掉落 / 自动装备 / 背包 / C 面板
- [x] 打击特效 VFX：弹弓拉弓+拖尾、杖冲击波、弹琴音波环+音符、飘字/暴击星/光柱
- [x] 关卡丰富化：游荡野狼 + 宝箱
- [x] 暗黑式鼠标操作：点地奔跑 / 点怪追击攻击 / 朝光标发射（PR #4）
- [x] 无头自测 coretest 39/39；engine/canvas/phaser 三套 tsc 通过

---

## M1 · 完全 Phaser 原生化 · 阶段1（已完成，PR #5）

**目标**：渲染基座从「混合架构（CanvasTexture 整帧上传）」改为 Phaser 原生。

- [x] 抽象 `ITextLayer` 接口（`text()` / `measureText()`）
- [x] `CanvasTextLayer`（Canvas 2D 文本实现，浏览器/微信版复用）
- [x] `PhaserPainter`：`IPainter` 的 `Phaser.GameObjects.Graphics` 实现（矩形/圆/椭圆/多边形/弧/描边 + 颜色解析）
- [x] `PhaserTextLayer`：池化 `Phaser.Text`（`begin/acquire/end`，离屏量取宽度）
- [x] `CanvasGame` 后端无关化：构造可选 `ctx` + `setBackend()`；拆 `step()/cameraScroll()/renderWorld()/renderUI()`
- [x] `WorldScene`：世界坐标绘制 + Phaser 相机 `setScroll` 跟随 + 键鼠输入接入
- [x] `UIScene`：HUD/对白/面板/结算屏幕层叠加（相机不滚动）
- [x] `main.ts` 装载 `[WorldScene, UIScene]`；移除旧 `GameScene`
- [x] **验收**：coretest 39/39；三套 tsc 通过；浏览器 WebGL 实测渲染/相机/点地奔跑/点怪发射/命中粒子正常、无报错

---

## M2 · 完全 Phaser 原生化 · 阶段2（✅ 完成：粒子 + 相机特效）

**目标**：把自绘 VFX 与受击反馈替换/增强为 Phaser 原生粒子 / 相机能力。
> 采用「原生粒子叠加在自绘 VFX 之上」的增强方案：自绘图元保证与 Canvas 版观感一致、零回归；原生粒子提供 WebGL 批量打击感。`sparks`(命中) / `motes`(升级·暴击 金色) / `beams`(掉落光柱) 三个原生发射器。

- [x] 命中迸溅 → `ParticleEmitter`（`sparks.explode`，按命中点世界坐标）
- [x] 升级光环 → 金色粒子爆发（`motes.explode` + `camera.flash` 金闪）；暴击也喷金色光点
- [x] 掉落光柱 → 粒子上升光束（`beams`，`particleTint` 按物品稀有度着色）
- [x] 受击红闪 → `camera.flash`（受击帧触发）
- [x] 重击 / 震屏 → `camera.shake`（core.shake 触发）
- [x] 夜战压暗 → 原生 `Phaser.GameObjects.Rectangle` 固定层（`setScrollFactor(0)`，仅暗世界、HUD 保持明亮）；并从 `renderUI` 移除原自绘全屏 dim
- [x] **验收**：coretest 39/39；三套 tsc 通过；浏览器 WebGL 实测：启动无报错、原生渲染一致、相机跟随、运行时强制 q3 验证夜战压暗正确、motes/beams 发射器运行时 explode 无报错
- [x] 攻击拖尾（弹弓飞石）→ 原生粒子拖尾（`trail` 发射器，逐帧 `emitParticleAt(pr.x, pr.y)`；`CanvasGame.nativeProjectileTrail=true` 关闭自绘拖尾，仅浏览器/微信 Canvas 路径保留自绘）

---

## M3 · 完全 Phaser 原生化 · 阶段3（✅ 完成：多场景互通大世界骨架）

**目标**：以 Phaser 原生多 `Scene` 搭起「世界地图枢纽 ↔ 各章节地图」的互通骨架，
逻辑核心 `GameCore`（39/39）零改动，世界结构与进度交给可单测的 `WorldEngine`。
至此「完全迁移到 Phaser」三阶段（渲染/相机·粒子/多场景）全部完成。

- [x] `WorldEngine`（`engine/WorldEngine.ts`，引擎无关·可单测）：节点图谱 + 互通邻接 + 解锁门控(`requires`) + 访问/通关记录 + `canEnter/enter/unlock/complete/neighbors` + 进度快照 `saveSnapshot/loadSnapshot` + `serialize/restore`
- [x] `WorldData`（`engine/WorldData.ts`）：大卫一生世界节点图谱（伯利恒·牧场 / 旷野·探索 可玩；以拉谷·歌利亚 / 基比亚 / 亚杜兰洞 / 洗革拉 / 希伯仑 为占位节点 `scene=null`，按圣经主线 `requires` 逐图解锁）
- [x] `WorldMapScene`（Phaser 原生枢纽）：节点用 `Graphics`+`Text` 绘制，连线表互通，按解锁/访问/通关着色（🔒锁定 / ✓通关 / …占位），点击已解锁真实场景即 `scene.start` 切入
- [x] 新增第二张可互通地图 `FieldScene`（旷野）：复用 `PhaserPainter`+`Sprites` 渲染独立瓦片地图，方向键/WASD 走位，瓦片碰撞
- [x] 场景切换：枢纽 → `WorldScene`(第一章) / `FieldScene`(旷野)；地图内按 **M** 返回枢纽；旷野西侧「返回世界地图」传送点（踩上即返回）
- [x] 切图保持游戏状态不丢：共享 `WorldEngine` 实例存于游戏级 `registry`（`worldEngine` 键），往返复用同一实例；第一章通关 → `complete('bethlehem')` 解锁以拉谷
- [x] 相机边界按当前地图尺寸 `setBounds`（旷野 34×26 瓦片 `setBounds(0,0,FMAP_W,FMAP_H)` + 跟随钳制）
- [x] `main.ts` 注册 `[WorldMapScene, WorldScene, UIScene, FieldScene]`，启动场景=世界地图枢纽
- [x] coretest 增量补 `WorldEngine` 单测（解锁/访问/通关/占位门控/邻接/快照/序列化恢复）→ **51/51**（原 39 不破 + 新增 12）
- [x] **验收**：枢纽 ↔ 牧场 ↔ 旷野 互通切图正常、相机正确、进度经 WorldEngine 保持；coretest 51/51；三套 tsc 通过；浏览器 WebGL 实测无报错

---

## M4 · HeroEngine 英雄卡 + 带兵同伴（待办）

- [ ] `HeroCard` 数据结构 + 分级属性/成长表（见 §13）
- [ ] 英雄卡数据：先落地三勇士 + 核心三将（其余分批补）
- [ ] 招募事件框架（事件触发 → 入队）
- [ ] 出战名单（大卫 + 至多 N 名）与切换 UI
- [ ] 同伴 AI：跟随 / 索敌 / 攻击 / 各自升级
- [ ] 英雄卡面板（查看属性/能力/等级）
- [ ] **验收**：能招募 ≥1 名勇士并带其打怪、同伴正常作战升级；coretest 全绿

---

## M5 · WorldEngine 互通世界 / 切图 / 解锁（待办）

- [ ] `WorldGraph` 地图节点图 + 相邻边
- [ ] 解锁机制：主线门控 vs 探索发现
- [ ] 已解锁地图间传送 / 回访
- [ ] 探索完成度 `discovered(mapId)`
- [ ] **验收**：多地图互通、解锁与回访符合预期

---

## M6 · QuestEngine 主线事件 + 第二章「歌利亚」（待办）

- [ ] `QuestEngine` 主线状态机 + 支线 + 事件触发
- [ ] 第二章「歌利亚」地图 + 任务 + Boss 战
- [ ] 与英雄/世界引擎打通（事件解锁勇士/地图）
- [ ] **验收**：第二章可从第一章推进进入并通关

---

## M7 · 内容扩充至「大卫一生」（待办）

- [ ] 妻子相遇解锁事件（米甲/亚比该/亚希暖/拔示巴，严肃处理）
- [ ] 勇士相遇解锁事件（按圣经名册分批）
- [ ] 先知系统：医治 / 驱魔 / 求问（撒母耳/拿单/迦得/亚比亚他）
- [ ] 主线 12 幕时间线逐幕落地（牧童→歌利亚→逃亡聚众→登基→迎约柜→勇士传奇→拔示巴与拿单→押沙龙之乱→暮年立所罗门）
- [ ] 存档引擎 SaveEngine（序列化进度/背包/英雄/解锁）
- [ ] **验收**：可贯穿主线体验大卫一生关键事件，直到年老

---

## 变更日志（每次 push 追加一行）

| 日期 | 提交 | 内容 |
|---|---|---|
| 2026-05-30 | PR #5 | 完成 M1 阶段1（PhaserPainter + WorldScene/UIScene + 原生相机/粒子基座） |
| 2026-05-30 | (本次) | 新建本进度看板 + 完善 docs/开发计划.md（写入完全 Phaser 原生化方案 + 全部需求） |
| 2026-05-30 | (本次) | 把 PR #5 rebase 到集成分支 devin/1780127785-cocos-david-port（PR#2/#4 已合并）、改基底、解冲突，无冲突可合并 |
| 2026-05-30 | (本次) | M2 阶段2：新增 motes(升级·暴击金色) + beams(掉落光柱按稀有度着色) 原生发射器；升级金闪。coretest 39/39、phaser tsc、浏览器启动无报错 |
| 2026-05-30 | (本次) | M2 阶段2：夜战压暗改为原生 Rectangle 固定层(仅暗世界、HUD 明亮)，renderUI 移除自绘 dim。运行时强制 q3 验证夜战正确、发射器 explode 无报错。M2 核心完成 88% |
| 2026-05-30 | (本次) | M2 收尾：弹弓飞石拖尾粒子化(trail 发射器逐帧 emitParticleAt)，CanvasGame.nativeProjectileTrail 关闭 Phaser 路径自绘拖尾。coretest 39/39、三套 tsc、浏览器实测 emit 无报错。**M2 ✅ 100% 完成** |
