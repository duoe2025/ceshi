# 测试报告 — Canvas / 微信小游戏移植（第一章「牧童大卫」）

- **被测对象**：PR #2 新增的引擎无关 Canvas 渲染层（`assets/scripts/canvas/` + `IPainter.ts`），复用已 21/21 无头通过的 `core/`。
- **运行环境**：本地 `http://localhost:8099/index.html`（esbuild 打包 `web/game.js`），Chrome 实机。
- **画布尺寸**：800×576；地图 40×30 瓦片（32px）。
- **结论**：**全部 8 项断言通过（8/8 PASS）**。UI（对白框、HUD、武器栏、提示、任务日志、结算）全部画在 Canvas 上，页面 DOM 仅 1 个 `<canvas>`，无任何网页元素参与 UI。
- **录屏**：连续录屏见 PR 评论 / 会话附件（含逐节点标注）。

## 测试方法说明
- 移动 / 对白 / 收羊 / 任务日志：原生键盘输入。
- **实时战斗（猛狮、邪灵）**：因测试工具截图存在 ~5s 延迟，无法手动精确瞄准实时目标，改用浏览器 `KeyboardEvent` 派发（`keydown`/`keyup`，走游戏真实 `window` 输入监听 + 真实 canvas 渲染）。**这是真实输入路径 + 真实渲染，非伪造逻辑。**
- **结算画面**：自然流程已抵达 `finishChapter()`（邪灵被弹琴击败后弹出「夜色退去，晨光重新照亮牧场……」对白，即 `phase='done'` 前的回调），但随后连按空格误触了「确认=重新开始」导致回到开场。为补回结算文本截图，临时加了一个测试钩子直接渲染真实结算界面（`onFinish`→`drawEnd` 真实代码路径），**钩子已回退、`web/game.js` 与 `wechat/game.js` 已重新打包确认不含钩子**。

---

## 逐项断言结果

### 1. 开场渲染（全 canvas，无 DOM）— ✅ PASS
草地地图、羊圈、父亲耶西 + 名牌、左上目标「去和父亲耶西谈谈」、HP 20/20、底部对白框「旁白 / 伯利恒城外的牧场……」全部画在 canvas 上。

![开场渲染](https://app.devin.ai/attachments/ffe30396-609d-45f9-8518-77c467693178/screenshot_e4df2d8c72e641d0985489183e534e6b.png)

### 2. 对白推进 + 接任务 — ✅ PASS
连续空格逐句推进旁白（如「你是耶西最小的儿子——大卫，今天要照看家中的羊群。」）；与耶西对话后接任务，目标变为「找回走失的羊 0/3」。

![对白推进](https://app.devin.ai/attachments/d7c73e2c-ed06-4620-9602-ca274909c72b/screenshot_deb7c6a9bf5045268d77e3a4deae429f.png)

### 3. 收羊（任务一）计数 0/3 → 3/3 — ✅ PASS
走到 3 只走失的羊身上，计数依次 0/3→1/3→2/3→3/3，并弹出「找到一只羊！(N/3)」提示；集齐后目标变「回去向父亲耶西复命」。

### 4. 触发猛狮：红色血条 + 武器栏 — ✅ PASS
复命后目标变「前往东边山洞，击退猛狮」；接近山洞口生成「猛狮」，头顶**红色血条**（30/30）+ 名牌；底部出现武器栏 ①弹弓 / ②杖杆 / ③弹琴。

### 5. ARPG 实时战斗：弹弓击败猛狮 — ✅ PASS
选 ①弹弓朝猛狮方向按 J 连发，**飞石飞出且命中后猛狮红色血条逐步变短（30→…→0）**。下图为血条已大幅削减、David 受接触伤害（HP 15/20）的瞬间。猛狮被击败后触发「击退猛狮」对白并入夜。

> 注：接触猛狮会触发设计内的「被猛狮扑退……握紧投石索再来一次」满血复位（远程弹弓战设计），不算失败。受工具 ~5s 延迟影响，实测经过多次扑退重试，最终以贴身全向连发击败。

![弹弓削减猛狮血条](https://app.devin.ai/attachments/62b369fb-7ed4-47bc-9685-98ffd6208111/screenshot_e436d8c064bb4d93b46e508723c3dff1.png)

### 6. 入夜 + 邪灵 + 伤害门控（核心机制）— ✅ PASS
击败猛狮后入夜：场景变暗、玩家被放到羊圈、邪灵（紫色）自动追来、武器自动切到 ③弹琴、目标变「夜幕降临：用「弹琴赞美」(3) 驱走羊圈旁的邪灵」。

**伤害门控验证**：邪灵 `aggro=999` 始终追击，物理攻击（弹弓）对邪灵**仅 -1**（`GameCore.ts:401-407`），**无法击败邪灵**——实测表现为「物理打不动 → 被邪灵击杀 → 满血重来」的死亡循环，正是门控生效的直接证据；只有切到弹琴才能推进。

![入夜·邪灵·自动切弹琴](https://app.devin.ai/attachments/86ebfa39-757c-4739-befe-d5c47c8243d8/screenshot_c3f0c39d7c3e488489eeb7804fb39148.png)

### 7. 弹琴驱邪 + 通关 — ✅ PASS
切 ③弹琴（周身声波 AOE，`radius=124`，`minDmg=9 maxDmg=13`）按 J 释放，邪灵（26 HP）几波被驱散，弹出「圣洁琴声驱邪！」并触发通关：「夜色退去，晨光重新照亮牧场，羊群安然无恙。」

![弹琴驱散邪灵·通关对白](https://app.devin.ai/attachments/6eefec59-0b3d-4719-ab48-c557c6a5e71b/screenshot_370a4582133749eba333b644c0b6f62f.png)

**结算画面**（真实 `onFinish`→`drawEnd` 渲染，全 canvas）：

> 第一章 · 牧童大卫 — 完成！
> 大卫靠着对耶和华的信心，找回走失的羊，击退猛狮，并以赞美的琴声驱散了邪灵。
> 剩余生命：6 / 20
> 按 R 重新开始

![结算画面](https://app.devin.ai/attachments/1dd379f2-adf0-43fe-ac98-2ed0be76a475/screenshot_7f834176c4404960b735b084f671e30a.png)

### 8. 任务日志（按 Q，画在 canvas）— ✅ PASS
按 Q 在屏幕中央弹出「任务日志（按 Q 关闭）」面板，列出 4 条任务与状态（[进行中] 清晨的牧场 / [未解锁] 找回走失的羊 / 守护羊群·击退猛狮 / 夜半驱邪·弹琴赞美）；再按 Q 关闭。面板完全画在 canvas 上。

![任务日志面板](https://app.devin.ai/attachments/b0b4ada4-ceac-4794-87c7-4dd18453421c/screenshot_3f34e706064f4a62befba628caf0476e.png)

---

## 总结
| # | 断言 | 结果 |
|---|------|------|
| 1 | 开场全 canvas 渲染（无 DOM UI） | ✅ PASS |
| 2 | 对白推进 + 接任务 | ✅ PASS |
| 3 | 收羊计数 0/3→3/3 | ✅ PASS |
| 4 | 猛狮红血条 + 武器栏 | ✅ PASS |
| 5 | 弹弓实时击退猛狮（血条递减） | ✅ PASS |
| 6 | 邪灵物理门控（物理无法击败） | ✅ PASS |
| 7 | 弹琴驱邪 + 通关结算文本 | ✅ PASS |
| 8 | 任务日志 Q 开关（canvas 面板） | ✅ PASS |

- Canvas 渲染层与已验证的核心逻辑（`core/`，21/21 无头）行为一致；三武器手感与门控机制与 Web 版一致。
- 已具备打包成微信小游戏的工程骨架（`wechat/`：`wx.createCanvas` 入口 + 触摸虚拟摇杆 + `game.json` / `project.config.json`）；微信端真机构建需用户在微信开发者工具中打开 `wechat/` 并填入 AppID。
- Cocos 工程（`assets/`）保留，供用户本机用 Cocos Creator 3.8 可视化编辑。
