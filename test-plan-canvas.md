# 测试计划 — Canvas / 微信小游戏移植（PR #2 新增 Canvas 层）

## 被测对象
新增的引擎无关 Canvas 渲染层：`assets/scripts/canvas/{CanvasPainter,CanvasGame,main.web,main.wechat}.ts`，
复用 `core/`（已 21/21 无头通过）+ `Sprites.ts`（重构为 IPainter，颜色用 hex 字符串）。
浏览器版打包为 `web/game.js`，本地以 `http://localhost:8099/index.html` 运行。

关键变化：UI（对白框、HUD、武器栏、提示、任务日志、结算）从 DOM 改为**全部画在 Canvas 上**（微信无 DOM 约束）。
若渲染层坏掉，画面会黑屏 / 缺文字 / 精灵错位 —— 与正常表现明显不同。

## 测试方法（重要）
- 移动/对白/收羊等用**原生键盘**（computer tool）操作，可解释性强。
- **实时战斗**（猛狮/邪灵）由于工具截图延迟 ~5s，无法手动精确瞄准实时目标。
  采用 `browser_console` **派发真实 KeyboardEvent**（`keydown`/`keyup`，走游戏真实输入监听 + 真实 canvas 渲染）来精确控制射击时机。
  这仍是「真实输入路径 + 真实渲染」，非伪造/模拟逻辑。报告中会明确说明。

## 代码依据（grep/read 已确认）
- 猛狮 q2：`GameData.lion = {c:33,r:9}` → 世界坐标 (1056,288)；`aggro=220 touchRange=28 atk=4 maxHp=30 spd=1.6`。
  弹弓 `projSpeed=7 hitR=22 minDmg=6 maxDmg=10` → 约 3~5 发击败。接触致死 → `playerDown()` 满血复位到 (832,704) 并显示「被猛狮扑退…握紧投石索再来一次」。**设计为远程弹弓战**。
- 邪灵 q3：击败猛狮 → `nightFall` 对白 → `phase='q3'`，玩家复位 (288,736)，`spawnEnemy('spirit')` 于 (288,672)，`weapon` 自动设为 `harp`，弹出提示「夜战！用「弹琴赞美」(3) 驱散邪灵」。
  邪灵 `aggro=999`（始终追击）`touchRange=32 maxHp=26`。物理（sling/staff）`damageEnemy`：仅 -1 + 提示「物理攻击对邪灵几乎无效！改用「弹琴赞美」(按 3)」；弹琴 `radius=124 minDmg=9 maxDmg=13` → 约 2~3 次驱散。

## 主流程（一条连续录屏）

1. **开场渲染** — 期望：草地地图、羊圈、父亲耶西+名牌、左上「目标：去和父亲耶西谈谈」、HP 20/20、底部对白框「旁白 / 伯利恒…」。失败判定：黑屏/无文字/精灵缺失。

2. **对白推进 + 接任务** — 连续空格推进。期望：对白逐句切换；结束后目标变为「找回走失的羊 0/3」。

3. **收羊（任务一）** — WASD/方向键走到 3 只走失的羊身上。期望：计数 0/3→1/3→2/3→3/3，羊跟随大卫。失败判定：踩到羊计数不变。

4. **触发猛狮（任务二）** — 集齐后回耶西处复命（空格）。期望：目标变「前往…击退猛狮」；接近山洞口生成「猛狮」，头顶**红色血条**+名牌；底部武器栏出现 ① 弹弓 / ② 杖杆 / ③ 弹琴。

5. **ARPG 实时战斗（弹弓击败猛狮）** — 选 ①弹弓，朝猛狮方向（面向对齐）按 J 连发，边后撤边打风筝。
   - 期望：飞石飞出；**命中后猛狮红色血条变短**（30→…→0）；最终猛狮被击败，触发「击退猛狮」对白并入夜。
   - 失败判定：无飞石 / 血条不动 / 物理也打不动。
   - 注：接触猛狮会致死复位（满血再战），属设计内行为，不算失败。

6. **入夜 + 邪灵 + 伤害门控（核心机制）** — 入夜后玩家被放到羊圈、邪灵（紫色血条）自动追来。**先按 ① 用弹弓物理打邪灵 1~2 下**。
   - 期望：邪灵血条几乎不动（每次仅 **-1**，26→25→24）；顶部提示「**物理攻击对邪灵几乎无效！改用「弹琴赞美」(按 3)**」。
   - 失败判定：物理把邪灵正常掉血（每次掉 6-10），则门控失效（bug）。

7. **弹琴驱邪 + 通关** — 按 ③ 切弹琴，靠近邪灵（≤124px）按 J 释放声波光环。
   - 期望：邪灵**大幅掉血**（每次 **-9~-13**，附「圣洁琴声驱邪！-N」提示），2~3 次驱散；显示通关结算文本「**第一章 · 牧童大卫 — 完成！**…剩余生命：N/20…按 R 重新开始」。
   - 失败判定：弹琴不掉血 / 无结算文本。

8. **任务日志（顺带验证 UI）** — 按 Q。期望：屏幕中央弹出任务日志面板（画在 canvas 上），列出任务状态；再按 Q 关闭。

## 通过标准
- 全程画面正常（地图/精灵/文字都在 canvas 上），无 DOM 元素参与 UI。
- 三武器手感与门控机制与 Web 版一致：弹弓击败猛狮；物理对邪灵 -1+提示；弹琴对邪灵 -9~13。
- 能从开场一路打到通关结算「第一章 · 牧童大卫 — 完成！」。
